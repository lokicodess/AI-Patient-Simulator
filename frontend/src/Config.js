const Config = {
    speech: {
      defaultRate: 0.6, // Default speech rate (0.1 to 10)
      defaultVoiceURI: '', // Default voice URI; leave empty to use the browser's default voice
    },
  };
  
  export default Config;